name: DIGIMON Temporary Windows Runner Stub

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: windows-latest
    steps:
      - name: Inert stub
        shell: pwsh
        run: Write-Host "DIGIMON temporary validation completed; no payload URL stored here."
